\documentclass[aspectratio=169, 11pt]{beamer}

\usetheme{Madrid}
\usecolortheme{whale}
\setbeamertemplate{navigation symbols}{}
\setcounter{tocdepth}{1}
\AtBeginSection{\begin{frame}\centering{\usebeamercolor[fg]{structure}\Huge\bfseries\insertsection}\end{frame}}

% Custom colors
\definecolor{tuwblue}{RGB}{0,102,153}
\definecolor{tuwgray}{RGB}{128,128,128}
\definecolor{successgreen}{RGB}{34,139,34}
\definecolor{failred}{RGB}{178,34,34}
\definecolor{valueorange}{RGB}{230,126,34}
\definecolor{policyviolet}{RGB}{117,80,123}

% Custom colors
\definecolor{tuwblue}{RGB}{0,102,153}
\definecolor{tuwgray}{RGB}{128,128,128}
\definecolor{successgreen}{RGB}{34,139,34}
\definecolor{failred}{RGB}{178,34,34}
\definecolor{valueorange}{RGB}{230,126,34}
\definecolor{policyviolet}{RGB}{117,80,123}

\setbeamercolor{title}{fg=white,bg=tuwblue}
\setbeamercolor{frametitle}{fg=white}
\setbeamercolor{structure}{fg=tuwblue}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc}

\title[PPO on CartPole]{Reinforcement Learning: PPO on CartPole}
\subtitle{Introduction to Deep Learning}

\author{%
  Lukas Leeb \and
  Michael Gail
}

\institute[TU Wien]{Technical University of Vienna}

\date{\today}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Outline}
  \tableofcontents
\end{frame}

\section{Introduction and Terms}

\subsection{Reinforcement Learning Definition}
\begin{frame}{Reinforcement Learning Definition}
  \begin{columns}
    \begin{column}{0.4\textwidth}
      \begin{itemize}
        \item No fixed input-output pairs to learn from
        \item Agent interacts with environment
        \item Receives reward to indicate if its actions are good or bad
        \item Learn a policy, that maximizes cumulative reward
      \end{itemize}
    \end{column}
    \begin{column}{0.6\textwidth}
      \begin{center}
      \begin{tikzpicture}[
          node distance=2.5cm,
          box/.style={rectangle, draw=tuwblue!80, thick, minimum width=2.8cm, minimum height=1.2cm, rounded corners},
          arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!80}
        ]
        \node[box] (agent) {\textbf{Agent}};
        \node[box, right=of agent] (env) {\textbf{Environment}};
        \draw[arrow, bend left=40] (agent.north) to node[above] {action} (env.north);
        \draw[arrow, bend left=40] (env.south) to node[below] {state, reward} (agent.south);
      \end{tikzpicture}
      \end{center}
    \end{column}
  \end{columns}
\end{frame}

\subsection{Reinforcement Learning Environment}
\begin{frame}{Reinforcement Learning Environment}
  An environment is modeled as a \textbf{Markov Decision Process (MDP)}

  \begin{itemize}
    \item discrete time steps $t = 0, 1, 2, 3, ...$
    \item At each time step
      \begin{itemize}
        \item environment state $S_t$
        \item reward $R_t$ (if $t \neq 0$)
        \item agent selects action $A_t$
      \end{itemize}
    \item resulting environment in next step is defined by
      \begin{itemize}
        \item probability distribution $p(s'|s,a)$
        \item reward function $r(s,a)$
      \end{itemize}
  \end{itemize}
\end{frame}

\subsection{CartPole Environment}
\begin{frame}{CartPole Environment\footnote[frame]{Gymnasium: \url{https://gymnasium.farama.org/environments/classic_control/cart_pole/}}}
  \begin{columns}
    \begin{column}{0.35\textwidth}
      \includegraphics[width=\textwidth]{assets/CartPole.png}
    \end{column}
    \begin{column}{0.65\textwidth}
      A pole is attached to a cart moving along a frictionless track.
      \vfill
      \textbf{Goal:} Keep the pole balanced by applying left or right
      forces.
      \vfill
      \textbf{Action space:} $0$ (left), $1$ (right)
      \vfill
      \textbf{Observation space:}
      \begin{itemize}
        \item Cart position $\in [-4.8, 4.8]$
        \item Cart velocity $\in (-\infty, \infty)$
        \item Pole angle $\in [-24^\circ, 24^\circ]$
        \item Pole angular velocity $\in (-\infty, \infty)$
      \end{itemize}
      \vfill
      \textbf{Reward:} $1$ per step while not terminated
      \vfill
      \textbf{Termination:}
      \begin{itemize}
        \item Pole angle exceeds $\pm 12^\circ$
        \item Cart leaves area $[-2.4,2.4]$
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

\subsection{Reinforcement Learning and PPO}
\begin{frame}{Reinforcement Learning and PPO}
  \centering
  This slide will explain where in the Reinforcement Learning Process PPO fits in.
\end{frame}

\section{Proximal Policy Optimization}

\subsection{Overview}
\begin{frame}{Overview\footnote[frame]{Schulman et al., \emph{Proximal Policy Optimization Algorithms}, arXiv:1707.06347v2 (2017).}}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{PPO is an on-policy actor--critic method}
      \vspace{0.5em}
      \begin{itemize}
        \item Alternate between \textbf{sampling} with the current policy and
              \textbf{optimizing} a surrogate objective
        \item \textbf{Actor} $\pi_\theta(a \mid s)$ chooses actions and is the
              policy we improve
        \item \textbf{Critic} $V_\phi(s)$ estimates state value, used to compute
              advantages $\hat{A}_t$
        \item Reuse each rollout for several mini-batch epochs, but keep the
              new policy close to the old one
      \end{itemize}
    \end{column}
    \begin{column}{0.52\textwidth}
      \centering
      \begin{tikzpicture}[
          scale=0.82,
          transform shape,
          box/.style={rectangle, draw=tuwblue!80, thick, rounded corners,
                      minimum height=0.75cm,
                      align=center, font=\small},
          smallbox/.style={rectangle, draw=tuwblue!60, thick, rounded corners,
                           minimum height=0.65cm,
                           align=center, font=\scriptsize},
          leftbox/.style={box, text width=2.75cm},
          rightbox/.style={box, text width=3.05cm},
          leftsmall/.style={smallbox, text width=2.75cm},
          rightsmall/.style={smallbox, text width=3.05cm},
          arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!70},
          node distance=0.55cm and 1.35cm
        ]
        \node[leftbox] (actor) {Actor\\$\pi_{\theta_{\mathrm{old}}}(a\mid s)$};
        \node[rightbox, right=of actor] (env) {Environment\\CartPole};
        \node[rightsmall, below=of env] (rollout) {rollout data\\$s_t,a_t,r_t$\\$\log\pi_{\theta_{\mathrm{old}}}$};
        \node[leftbox, below=of actor] (critic) {Critic\\$V_\phi(s)$};
        \node[rightsmall, below=of rollout] (adv) {advantage\\estimates $\hat{A}_t$};
        \node[leftsmall] (update) at (critic |- adv) {PPO update\\clipped surrogate};

        \draw[arrow] (actor.east) -- node[midway, above=2pt, font=\scriptsize] {actions} (env.west);
        \draw[arrow] (env.south) -- node[right, font=\scriptsize] {samples} (rollout.north);
        \draw[arrow] (rollout.south) -- (adv.north);
        \draw[arrow] (critic.east) -- node[midway, above=3pt, xshift=0.12cm, font=\scriptsize] {$V(s_t)$} (adv.west);
        \draw[arrow] (adv.west) -- (update.east);
        \draw[arrow] (update.north) -- node[left, font=\scriptsize] {fit $V$} (critic.south);
        \draw[arrow, rounded corners] (update.west) -- ++(-0.45,0) |- node[left, font=\scriptsize] {$\theta\leftarrow$} (actor.west);
      \end{tikzpicture}
      \vspace{0.4em}

      \small
      ``Proximal'' means the update is restricted: PPO removes incentive for
      probability ratios outside $[1-\epsilon,\,1+\epsilon]$.
    \end{column}
  \end{columns}
\end{frame}


\begin{frame}{PPO is Gradient-Based}
  PPO does not directly search over actions. It changes the neural-network
  parameters so that the policy distribution assigns better probabilities.

  \vspace{0.5em}
  \begin{block}{Optimization view}
    \[
      \theta,\phi
      \leftarrow
      \theta,\phi - \alpha\,\nabla_{\theta,\phi}\mathcal{L}^{\mathrm{PPO}}
    \]
  \end{block}

  \vspace{0.2em}
  \begin{center}
  \begin{tikzpicture}[
      box/.style={rectangle, draw=tuwblue!80, thick, rounded corners,
                  minimum width=2.6cm, minimum height=0.75cm, align=center,
                  font=\small},
      arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!70},
      node distance=0.55cm and 0.75cm
    ]
    \node[box] (batch) {rollout\\mini-batch};
    \node[box, right=of batch] (loss) {PPO loss\\$\mathcal{L}^{\mathrm{PPO}}$};
    \node[box, right=of loss] (grad) {backprop\\gradients};
    \node[box, right=of grad] (adam) {Adam\\update};
    \draw[arrow] (batch) -- (loss);
    \draw[arrow] (loss) -- (grad);
    \draw[arrow] (grad) -- (adam);
  \end{tikzpicture}
  \end{center}

  \begin{itemize}
    \item Actor parameters $\theta$: change action probabilities
    \item Critic parameters $\phi$: improve value estimates
    \item The PPO paper writes objectives to maximize; in code we minimize the
          negative objective with Adam
  \end{itemize}
\end{frame}


\subsection{Objective}
\begin{frame}{Objective: Policy Loss}
  The actor is updated with the clipped surrogate objective from PPO.

  \vspace{0.6em}
  \begin{block}{Clipped policy objective}
    \begin{align*}
      L_t^{\mathrm{CLIP}}(\theta)
      &= \min\!\left(
          r_t(\theta)\hat{A}_t,
          \operatorname{clip}\bigl(r_t(\theta), 1-\epsilon, 1+\epsilon\bigr)\hat{A}_t
        \right) \\
      r_t(\theta)
      &= \frac{\pi_\theta(a_t\mid s_t)}{\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)}
    \end{align*}
  \end{block}

  \begin{itemize}
    \item $r_t(\theta)$ compares the new policy to the policy that collected the rollout
    \item $\hat{A}_t$ tells whether the sampled action was better or worse than expected
    \item Clipping removes incentive for overly large policy changes
  \end{itemize}
\end{frame}

\begin{frame}{Objective: Value Loss}
  The critic is trained as a regression problem: predict the return target for
  each sampled state.

  \vspace{0.6em}
  \begin{block}{Value-function loss}
    \begin{align*}
      L_t^{\mathrm{VF}}(\phi)
      &= \left(V_\phi(s_t) - V_t^{\mathrm{target}}\right)^2
    \end{align*}
  \end{block}

  \begin{itemize}
    \item $V_\phi(s_t)$ estimates how much return is expected from state $s_t$
    \item $V_t^{\mathrm{target}}$ is computed from rollout rewards and bootstrapping
    \item A better critic gives better advantage estimates and reduces update variance
  \end{itemize}
\end{frame}

\begin{frame}{Objective: Entropy Bonus}
  PPO also rewards stochastic policies to avoid collapsing to one action too
  early.

  \vspace{0.6em}
  \begin{block}{Entropy bonus}
    \begin{align*}
      S[\pi_\theta](s_t)
      &= -\sum_a \pi_\theta(a\mid s_t)\log \pi_\theta(a\mid s_t)
    \end{align*}
  \end{block}

  \vspace{0.4em}
  \begin{block}{Combined objective}
    \vspace{-0.4em}
    \[
      L_t^{\mathrm{PPO}}(\theta, \phi)
      = L_t^{\mathrm{CLIP}}(\theta)
        - c_1 L_t^{\mathrm{VF}}(\phi)
        + c_2 S[\pi_\theta](s_t)
    \]
    \vspace{-0.8em}
  \end{block}
\end{frame}


\subsection{Advantage Estimation}
\begin{frame}{Advantage Estimation: Why Advantages?}
  The policy loss needs a signal for whether the sampled action was good.
  Instead of using raw return, PPO uses an \textbf{advantage estimate}:

  \vspace{0.4em}
  \begin{block}{General idea}
    \[
      \hat{A}_t \approx
      \underbrace{\widehat{Q}_t}_{\text{rollout estimate}}
      -
      \underbrace{V_\phi(s_t)}_{\text{critic baseline}}
    \]
  \end{block}

  \begin{itemize}
    \item $\widehat{Q}_t$: estimated from rewards after taking $a_t$ in the rollout
    \item $V_\phi(s_t)$: critic's baseline expectation for state $s_t$
    \item $\hat{A}_t > 0$: action was better than expected $\Rightarrow$ make it more likely
    \item $\hat{A}_t < 0$: action was worse than expected $\Rightarrow$ make it less likely
  \end{itemize}
\end{frame}

\begin{frame}{Advantage Estimation: Rollout Return}
  A direct estimate compares a discounted rollout return with the critic's
  current value estimate.

  \vspace{0.5em}
  \begin{block}{Truncated Monte-Carlo-style estimate}
    \begin{align*}
      G_t &= \sum_{l=0}^{T-t-1} \gamma^l r_{t+l}
             + \gamma^{T-t} V_\phi(s_T) \\
      \hat{A}_t &= G_t - V_\phi(s_t)
    \end{align*}
  \end{block}

  \begin{itemize}
    \item $t$ is the current position in a rollout segment of length $T$
    \item $l=0,\ldots,T-t-1$ iterates over future rewards
          $r_t, r_{t+1}, \ldots, r_{T-1}$
    \item Uses real rewards from the sampled trajectory segment
    \item Bootstraps with $V_\phi(s_T)$ because PPO uses fixed-length rollouts
    \item Often low bias, but can have high variance
  \end{itemize}
\end{frame}

\begin{frame}{Advantage Estimation: Bootstrapping}
  \small
  A rollout can stop because we reached the fixed segment length, even if the
  episode is not finished. Then the future after $s_T$ is unobserved.

  \vspace{0.5em}
  \centering
  \begin{tikzpicture}[
      state/.style={circle, draw=tuwblue!80, thick, minimum size=0.72cm, font=\scriptsize},
      reward/.style={rectangle, draw=tuwblue!60, rounded corners, minimum width=0.65cm,
                     minimum height=0.45cm, font=\scriptsize},
      arrow/.style={-{Latex[length=1.6mm]}, thick, tuwblue!70},
      node distance=0.45cm
    ]
    \node[state] (st) {$s_t$};
    \node[reward, right=of st] (rt) {$r_t$};
    \node[state, right=of rt] (st1) {$s_{t+1}$};
    \node[right=0.25cm of st1] (dots) {$\cdots$};
    \node[reward, right=0.25cm of dots] (rT) {$r_{T-1}$};
    \node[state, right=of rT] (sT) {$s_T$};
    \node[reward, right=0.85cm of sT, dashed, draw=failred!70] (future) {$?$};
    \node[below=0.25cm of future, font=\scriptsize, text=failred] {unobserved future};
    \node[reward, below=0.72cm of sT, draw=successgreen!80, text width=1.35cm,
          align=center] (boot) {$V_\phi(s_T)$};

    \draw[arrow] (st) -- (rt);
    \draw[arrow] (rt) -- (st1);
    \draw[arrow] (st1) -- (dots);
    \draw[arrow] (dots) -- (rT);
    \draw[arrow] (rT) -- (sT);
    \draw[arrow, dashed, failred!70] (sT) -- (future);
    \draw[arrow, successgreen!80] (sT) -- (boot);
  \end{tikzpicture}

  \vspace{0.4em}
  \begin{block}{Replace the unknown tail by the critic's estimate}
    \[
      G_t = \underbrace{r_t + \gamma r_{t+1} + \cdots + \gamma^{T-t-1} r_{T-1}}_{\text{observed rewards}}
            + \gamma^{T-t}\underbrace{V_\phi(s_T)}_{\text{estimated future}}
    \]
  \end{block}

  \vspace{-0.2em}
  \begin{itemize}
    \item This replacement is called \textbf{bootstrapping}
    \item If $s_T$ is terminal, the estimated future is $0$
  \end{itemize}
\end{frame}

\begin{frame}{Advantage Estimation: GAE}
  Generalized Advantage Estimation (GAE) smooths many temporal-difference errors
  into one advantage estimate.

  \vspace{0.5em}
  \begin{block}{GAE estimate}
    \begin{align*}
      \delta_t &= r_t + \gamma V(s_{t+1}) - V(s_t) \\
      \hat{A}_t^{\mathrm{GAE}}
      &= \sum_{l=0}^{T-t-1} (\gamma\lambda)^l \delta_{t+l}
    \end{align*}
  \end{block}

  \begin{itemize}
    \item The sum again iterates over future offsets
          $l=0,\ldots,T-t-1$ inside the rollout
    \item $\delta_t$ measures one-step surprise relative to the critic
    \item $\lambda$ controls how far future TD errors influence $\hat{A}_t$
    \item Lower $\lambda$: lower variance; higher $\lambda$: lower bias
  \end{itemize}

  \vspace{0.2em}
  \centering
  \footnotesize Critic target used later: $V_t^{\mathrm{target}}=\hat{A}_t+V(s_t)$.
\end{frame}

\begin{frame}{Advantage Estimation: What Does $\lambda$ Do?}
  In GAE, $\lambda$ controls how much we trust short-term critic estimates
  versus longer rollout returns.

  \vspace{0.5em}
  \begin{block}{Future TD errors are exponentially weighted}
    \[
      \hat{A}_t^{\mathrm{GAE}}
      = \delta_t
        + (\gamma\lambda)\delta_{t+1}
        + (\gamma\lambda)^2\delta_{t+2}
        + \cdots
    \]
  \end{block}

  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{Small $\lambda$}\\[0.2em]
      \begin{itemize}
        \item mostly one-step TD errors
        \item relies more on critic predictions
        \item lower variance, more bias
      \end{itemize}
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{Large $\lambda$}\\[0.2em]
      \begin{itemize}
        \item includes more future rewards
        \item closer to Monte-Carlo return
        \item lower bias, more variance
      \end{itemize}
    \end{column}
  \end{columns}

  \vspace{0.3em}
  \centering
  \small $\lambda=0$: one-step TD estimate \qquad $\lambda=1$: rollout-return-like estimate
\end{frame}

\subsection{Clipped vs.\ Unclipped Objective}
\begin{frame}{Clipped vs.\ Unclipped Objective}
  \small
  PPO reuses the same rollout for several gradient steps. The ratio measures how
  much the new policy changed on the sampled action:
  \[
    r_t(\theta)=\frac{\pi_\theta(a_t\mid s_t)}{\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)}
    \qquad \text{and initially } r_t=1.
  \]

  \vspace{-0.3em}
  \begin{columns}[T]
    \begin{column}{0.49\textwidth}
      \begin{block}{Unclipped surrogate}
        \begin{minipage}[c][1.55cm][c]{\textwidth}
          \[
            L_t^{\mathrm{unclip}}(\theta)=r_t(\theta)\hat{A}_t
          \]
        \end{minipage}
      \end{block}
      \begin{itemize}
        \item If $\hat{A}_t>0$: increase action probability
        \item If $\hat{A}_t<0$: decrease action probability
        \item Problem: nothing stops very large policy changes
      \end{itemize}
    \end{column}
    \begin{column}{0.49\textwidth}
      \begin{block}{Clipped PPO surrogate}
        \begin{minipage}[c][1.55cm][c]{\textwidth}
          \[
            L_t^{\mathrm{CLIP}}(\theta)=
            \min\!\left(
              r_t\hat{A}_t,
              \operatorname{clip}(r_t,1-\epsilon,1+\epsilon)\hat{A}_t
            \right)
          \]
        \end{minipage}
      \end{block}
      \begin{itemize}
        \item Allows updates near the old policy
        \item Removes incentive outside $[1-\epsilon,1+\epsilon]$
        \item $\epsilon$ controls how conservative the update is
      \end{itemize}
    \end{column}
  \end{columns}

  \vspace{0.2em}
  \centering
  \begin{tikzpicture}[
      scale=0.88,
      arrow/.style={-{Latex[length=1.8mm]}, thick, tuwblue!70},
      tick/.style={thick, tuwblue!70},
      label/.style={font=\scriptsize}
    ]
    \draw[arrow] (0,0) -- (7.0,0) node[right, label] {$r_t$};
    \draw[tick] (2,0.12) -- (2,-0.12) node[below, label] {$1-\epsilon$};
    \draw[tick] (3.5,0.12) -- (3.5,-0.12) node[below, label] {$1$};
    \draw[tick] (5,0.12) -- (5,-0.12) node[below, label] {$1+\epsilon$};
    \draw[very thick, successgreen!75] (2,0.28) -- (5,0.28);
    \node[label, successgreen!60!black] at (3.5,0.62) {trusted update region};
    \node[label, failred] at (1.0,0.28) {too far};
    \node[label, failred] at (6.0,0.28) {too far};
  \end{tikzpicture}
\end{frame}


\subsection{Training Loop}
\begin{frame}{Training Loop: One PPO Iteration}
  PPO alternates between collecting fresh on-policy data and reusing that data
  for a limited number of optimization steps.

  \vspace{0.6em}
  \begin{center}
  \begin{tikzpicture}[
      box/.style={rectangle, draw=tuwblue!80, thick, rounded corners,
                  minimum width=3.0cm, minimum height=0.9cm, align=center},
      arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!70},
      node distance=0.7cm
    ]
    \node[box] (old) {current policy\\$\pi_{\theta_{\mathrm{old}}}$};
    \node[box, right=of old] (rollout) {collect rollout\\$N\times T$ samples};
    \node[box, right=of rollout] (adv) {compute\\$\hat{A}_t, V_t^{\mathrm{target}}$};
    \node[box, below=of adv] (update) {mini-batch updates\\$K$ epochs};
    \node[box, left=of update] (new) {new policy\\$\pi_\theta$};

    \draw[arrow] (old) -- (rollout);
    \draw[arrow] (rollout) -- (adv);
    \draw[arrow] (adv) -- (update);
    \draw[arrow] (update) -- (new);
    \draw[arrow]
      (new.west) .. controls +(-1.0,0) and +(0,-1.0) ..
      node[pos=0.55, xshift=-0.2cm, left, font=\scriptsize] {$\theta_{\mathrm{old}}\leftarrow\theta$}
      (old.south);
  \end{tikzpicture}
  \end{center}

  \vspace{0.3em}
  \begin{itemize}
    \item Data is \textbf{on-policy}: collected with the old policy before the update
    \item The stored old log-probabilities define the ratio $r_t(\theta)$ during optimization
  \end{itemize}
\end{frame}

\begin{frame}{Training Loop: Parallel Rollout Collection}
  Instead of one environment, PPO commonly runs $N$ environments in parallel for
  $T$ steps. This gives a batch of $N\cdot T$ transitions per iteration.

  \vspace{0.4em}
  \begin{center}
  \begin{tikzpicture}[
      cell/.style={rectangle, draw=tuwblue!65, rounded corners,
                   minimum width=1.05cm, minimum height=0.5cm, font=\scriptsize},
      label/.style={font=\scriptsize},
      arrow/.style={-{Latex[length=1.6mm]}, thick, tuwblue!70}
    ]
    \foreach \row/\env in {0/1,1/2,2/\vdots,3/N} {
      \node[label] at (-1.05,-\row*0.7) {env $\env$};
      \node[cell] at (0,-\row*0.7) {$t$};
      \node[cell] at (1.35,-\row*0.7) {$t+1$};
      \node[cell] at (2.70,-\row*0.7) {$\cdots$};
      \node[cell] at (4.35,-\row*0.7) {$t+T-1$};
    }
    \draw[arrow] (-0.1,0.55) -- (4.65,0.55) node[right,label] {$T$ steps};
    \draw[arrow] (5.45,0.05) -- (5.45,-2.15) node[below, xshift=0.25cm, label] {$N$ envs};
  \end{tikzpicture}
  \end{center}

  \vspace{0.2em}
  For each transition PPO stores:
  \[
    s_t,\; a_t,\; r_t,\; \text{done}_t,\; \log\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t),\; V_\phi(s_t)
  \]
\end{frame}

\begin{frame}{Training Loop: From Rollout to Training Batch}
  After rollout collection, the data is converted into supervised-style targets
  for the actor and critic.

  \vspace{0.5em}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{Compute from rewards}\\[0.2em]
      \begin{itemize}
        \item bootstrap from $V_\phi(s_T)$ if needed
        \item compute advantages $\hat{A}_t$ \newline using MC return or GAE
        \item compute critic targets:
          \[
            V_t^{\mathrm{target}}=\hat{A}_t+V_\phi(s_t)
          \]
      \end{itemize}
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{Flatten into a batch}\\[0.2em]
      \begin{itemize}
        \item rollout shape: $N\times T$
        \item training batch size: $N\cdot T$
        \item shuffle and split into mini-batches
        \item optionally normalize $\hat{A}_t$ before updates
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Training Loop: Mini-batch Updates}
  PPO does not update once on the whole rollout. It shuffles the rollout batch,
  splits it into mini-batches, and performs several passes over the same data.

  \vspace{0.5em}
  \begin{center}
  \begin{tikzpicture}[
      scale=0.75,
      transform shape,
      box/.style={rectangle, draw=tuwblue!80, thick, rounded corners,
                  minimum width=2.25cm, minimum height=0.75cm, align=center,
                  font=\small},
      sample/.style={rectangle, draw=tuwblue!60, fill=tuwblue!8,
                     minimum width=0.42cm, minimum height=0.36cm},
      mb/.style={rectangle, draw=tuwblue!70, thick, rounded corners,
                 minimum width=1.35cm, minimum height=0.48cm, align=center,
                 font=\scriptsize},
      arrow/.style={-{Latex[length=1.8mm]}, thick, tuwblue!70},
      label/.style={font=\scriptsize, align=center}
    ]
    \node[box] (rollout) at (0,0) {rollout batch\\$N\cdot T$ transitions};

    \foreach \i/\x/\y in {1/3.00/0.28,2/3.65/0.28,3/4.30/0.28,4/4.95/0.28,
                           5/3.00/-0.28,6/3.65/-0.28,7/4.30/-0.28,8/4.95/-0.28} {
      \node[sample] at (\x,\y) {};
    }
    \node[label] (shuffle) at (3.98,-0.92) {shuffle};

    \node[mb] (mb1) at (7.35,0.58) {mini-batch 1};
    \node[mb] (mb2) at (7.35,0.00) {mini-batch 2};
    \node[mb, draw=none] (mbdots) at (7.35,-0.58) {$\vdots$};
    \node[mb] (mbB) at (7.35,-1.16) {mini-batch $B$};
    \node[label] at (7.35,-1.78) {split};

    \node[box] (adam) at (12.15,0) {Adam step\\update $\theta,\phi$};
    \node[box] (repeat) at (15.45,0) {repeat\\$K$ epochs};

    \draw[arrow] (rollout.east) -- node[above, label] {shuffle} (2.70,0);
    \draw[arrow] (5.30,0) -- node[above, label] {split} (mb2.west);
    \draw[arrow] (mb2.east) -- node[above, label] {one mini-batch\\= one step} (adam.west);
    \draw[arrow] (adam.east) -- (repeat.west);
    \draw[arrow, rounded corners]
      (repeat.south) -- (15.45,-2.15)
      -- node[below, label] {next mini-batch / epoch} (8.70,-2.15)
      -- (8.70,-1.16) -- (mbB.east);
  \end{tikzpicture}
  \end{center}

  \vspace{0.2em}
  \begin{itemize}
    \item One mini-batch gives one optimizer step
    \item Reusing data improves sample efficiency
    \item Clipping makes this reuse safer by limiting policy drift from
          $\pi_{\theta_{\mathrm{old}}}$
  \end{itemize}
\end{frame}

\begin{frame}{Training Loop: PPO Updates}
  The same rollout batch is reused for $K$ epochs, but the clipped objective keeps
  the new policy close to the policy that collected the data.

  \vspace{0.4em}
  \begin{block}{For each epoch and mini-batch}
    \begin{enumerate}
      \item Recompute $\log\pi_\theta(a_t\mid s_t)$ and $V_\phi(s_t)$ with current networks
      \item Form the ratio
        \[
          r_t(\theta)=\exp\!\left(\log\pi_\theta(a_t\mid s_t)
          -\log\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)\right)
        \]
      \item Optimize policy loss, value loss, and entropy bonus with Adam
    \end{enumerate}
  \end{block}

  \centering
  \small After the updates: $\theta_{\mathrm{old}}\leftarrow\theta$, collect a new rollout.
\end{frame}

\section{Experiment Setup}

\subsection{Network Architecture}
\begin{frame}{Network Architecture}
  \begin{columns}
    \begin{column}{0.5\textwidth}
      \centering
      \textbf{Actor} $\pi_\theta(a \mid s)$\\[0.5em]
      \begin{tikzpicture}[
          layer/.style={rectangle, draw=tuwblue!80, thick, minimum width=2.6cm,
                        minimum height=0.65cm, rounded corners, font=\small},
          arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!60},
          node distance=0.45cm
        ]
        \node[layer] (in)  {Input $(4)$};
        \node[layer, below=of in]  (h1) {Linear$(64)$ + Tanh};
        \node[layer, below=of h1]  (h2) {Linear$(64)$ + Tanh};
        \node[layer, below=of h2]  (out) {Linear$(2)$};
        \node[below=0.35cm of out, font=\small, text=tuwgray] (dist) {Categorical$(logits)$};
        \draw[arrow] (in) -- (h1);
        \draw[arrow] (h1) -- (h2);
        \draw[arrow] (h2) -- (out);
        \draw[arrow] (out) -- (dist);
      \end{tikzpicture}
    \end{column}
    \begin{column}{0.5\textwidth}
      \centering
      \textbf{Critic} $V_\theta(s)$\\[0.5em]
      \begin{tikzpicture}[
          layer/.style={rectangle, draw=tuwblue!80, thick, minimum width=2.6cm,
                        minimum height=0.65cm, rounded corners, font=\small},
          arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!60},
          node distance=0.45cm
        ]
        \node[layer] (in)  {Input $(4)$};
        \node[layer, below=of in]  (h1) {Linear$(64)$ + Tanh};
        \node[layer, below=of h1]  (h2) {Linear$(64)$ + Tanh};
        \node[layer, below=of h2]  (out) {Linear$(1)$};
        \node[below=0.35cm of out, font=\small, text=tuwgray] (val) {$V(s) \in \mathbb{R}$};
        \draw[arrow] (in) -- (h1);
        \draw[arrow] (h1) -- (h2);
        \draw[arrow] (h2) -- (out);
        \draw[arrow] (out) -- (val);
      \end{tikzpicture}
    \end{column}
  \end{columns}
\end{frame}

\subsection{Hyperparameter Sweep}
\begin{frame}{Hyperparameter Sweep}
  Grid search over three dimensions --- all other parameters fixed.
  \vspace{0.6em}
  \begin{columns}[T]
    \begin{column}{0.58\textwidth}
      \textbf{Varied across runs}\\[0.4em]
      \begin{tabular}{ll}
        \toprule
        Dimension & Values \\
        \midrule
        Policy loss
          & unclipped \\
          & clipped $\epsilon \in \{0.1,\, 0.2,\, 0.3\}$ \\[0.3em]
        Advantage estimator
          & Monte Carlo \\
          & GAE $\lambda \in \{0.90,\, 0.95,\, 0.97\}$ \\[0.3em]
        Rollout / update ratio
          & $(\text{rollout length},\, \text{epochs})$ pairs \\
          & $\in \{(128, 4), (256, 4), (128, 8)\}$ \\
        \bottomrule
      \end{tabular}
    \end{column}
    \begin{column}{0.42\textwidth}
      \textbf{Fixed}\\[0.4em]
      \begin{tabular}{lc}
        \toprule
        Parameter & Value \\
        \midrule
        Learning rate $\alpha$ & $2.5 \times 10^{-4}$ \\
        Discount factor $\gamma$ & $0.99$ \\
        Value coef.\ $c_1$ & $0.5$ \\
        Entropy coef.\ $c_2$ & $0.01$ \\
        Parallel envs $N$ & $8$ \\
        Mini-batch size & $256$ \\
        \bottomrule
      \end{tabular}
    \end{column}
  \end{columns}
  \vspace{0.5em}
  \small Also evaluated across training set sizes:
  $1\text{K},\; 10\text{K},\; 100\text{K},\; 500\text{K},\; 1\text{M},\; 5\text{M}$ timesteps.
\end{frame}

\subsection{Evaluation Metrics}
\begin{frame}{Evaluation Metrics}
  \begin{itemize}
    \item \textbf{Mean episode return} --- average reward collected per episode; maximum is $500$ per default
    \item \textbf{Standard Deviation} --- standard deviation across multiple seeds, to judge stability across seeds
    \item \textbf{Success rate} --- episode is defined as "solved", if return $\geq 475$

    \item \textbf{Final evaluation} --- $60$ episodes after training; report above metrics
  \end{itemize}
\end{frame}

\section{Results}

\begin{frame}{Ablation Study: What Mattered?}
  \scriptsize
  \textbf{Setup:} CartPole-v1, 1M timesteps, 4 seeds $(0,1,2,3407)$,
  60 evaluation episodes, success if return $\ge 475$.

  \vspace{0.4em}
  \noindent\hspace*{\fill}%
    \begin{minipage}[t]{0.31\textwidth}\vspace{0pt}
      \textbf{Clip range}\par\vspace{0.2em}
      {\raggedright\tiny\setlength{\tabcolsep}{2pt}\renewcommand{\arraystretch}{1.15}%
      \begin{tabular}{@{}ccc@{}}
        \toprule
        $\epsilon$ & Return & Success \\
        \midrule
        $0.1$ & $351.1 \pm 131.2$ & $0.46 \pm 0.42$ \\
        \textbf{$0.2$} & \textbf{$418.1 \pm 133.3$} & \textbf{$0.73 \pm 0.42$} \\
        $0.3$ & $334.3 \pm 169.0$ & $0.49 \pm 0.49$ \\
        \multicolumn{3}{c}{\phantom{$0.0 \pm 0.0$}} \\
        \bottomrule
      \end{tabular}\par
      }

      \vspace{0.5em}
      \textcolor{successgreen}{Best trade-off: $\epsilon=0.2$}
    \end{minipage}%
    \hspace*{\fill}%
    \begin{minipage}[t]{0.34\textwidth}\vspace{0pt}
      \textbf{Advantage estimator}\par\vspace{0.2em}
      {\raggedright\tiny\setlength{\tabcolsep}{2pt}\renewcommand{\arraystretch}{1.15}%
      \begin{tabular}{@{}ccc@{}}
        \toprule
        Estimator & Return & Success \\
        \midrule
        GAE $\lambda=0.90$ & $421.9 \pm 133.7$ & $0.75 \pm 0.43$ \\
        \textbf{GAE $\lambda=0.95$} & \textbf{$418.1 \pm 133.3$} & \textbf{$0.73 \pm 0.42$} \\
        GAE $\lambda=0.97$ & $289.4 \pm 117.3$ & $0.20 \pm 0.35$ \\
        MC & $348.2 \pm 112.9$ & $0.23 \pm 0.28$ \\
        \bottomrule
      \end{tabular}\par
      }

      \vspace{0.5em}
      \textcolor{successgreen}{GAE beats MC in the final run}
    \end{minipage}%
    \hspace*{\fill}%
    \begin{minipage}[t]{0.31\textwidth}\vspace{0pt}
      \textbf{Rollout / update}\par\vspace{0.2em}
      {\raggedright\tiny\setlength{\tabcolsep}{2pt}\renewcommand{\arraystretch}{1.15}%
      \begin{tabular}{@{}ccc@{}}
        \toprule
        Steps:epochs & Return & Success \\
        \midrule
        $64{:}4$ & $146.5 \pm 29.1$ & $0.00 \pm 0.00$ \\
        $128{:}4$ & $418.1 \pm 133.3$ & $0.73 \pm 0.42$ \\
        \textbf{$256{:}4$} & \textbf{$500.0 \pm 0.0$} & \textbf{$1.00 \pm 0.00$} \\
        $128{:}8$ & $429.4 \pm 55.7$ & $0.68 \pm 0.25$ \\
        \bottomrule
      \end{tabular}\par
      }

      \vspace{0.5em}
      \textcolor{successgreen}{Longer rollouts solved CartPole}
    \end{minipage}%
  \hspace*{\fill}\par

  \vspace{0.9em}
  \centering
  \begin{tikzpicture}[
      scale=0.92,
      transform shape,
      card/.style={rectangle, rounded corners, draw=tuwblue!75, thick,
                   fill=tuwblue!5, minimum width=2.35cm, minimum height=0.95cm,
                   align=center, font=\scriptsize},
      result/.style={rectangle, rounded corners, draw=successgreen!80!black,
                     ultra thick, fill=successgreen!12, minimum width=2.95cm,
                     minimum height=1.15cm, align=center, font=\scriptsize\bfseries},
      plus/.style={font=\Large\bfseries, text=tuwblue},
      arrow/.style={-{Latex[length=2mm]}, ultra thick, successgreen!70!black}
    ]
    \node[font=\bfseries\small, text=tuwblue] at (0,0.95)
      {Winning recipe: constrain updates, estimate advantages well, collect enough context};

    \node[card] (clip) at (-4.20,0) {conservative\\clip $\epsilon=0.2$};
    \node[plus] at (-2.775,0) {$+$};
    \node[card] (gae) at (-1.35,0) {low-variance\\GAE $\lambda=0.95$};
    \node[plus] at (0.075,0) {$+$};
    \node[card] (roll) at (1.50,0) {longer rollout\\$256{:}4$};
    \node[result] (res) at (4.85,0) {SOLVED\\$500.0\pm0.0$ return\\$100\%$ success};
    \draw[arrow] (roll.east) -- (res.west);
  \end{tikzpicture}
\end{frame}

\begin{frame}{Results: Learning Progress}
  \begin{columns}[T]
    \begin{column}{0.68\textwidth}
      \centering
      \begin{tikzpicture}[x=1cm,y=1cm]
        \def\plotw{8.8}
        \def\ploth{4.8}
        \def\threshold{4.385}

        \foreach \x/\lab in {0/0,1.80/100,3.61/200,5.41/300,7.21/400} {
          \draw[tuwgray!18] (\x,0) -- (\x,\ploth);
          \node[font=\scriptsize, below] at (\x,0) {\lab};
        }
        \foreach \y/\lab in {0/0,0.92/100,1.85/200,2.77/300,3.69/400,4.62/500} {
          \draw[tuwgray!18] (0,\y) -- (\plotw,\y);
          \node[font=\scriptsize, left] at (0,\y) {\lab};
        }

        \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (\plotw+0.25,0)
          node[below, font=\scriptsize] {PPO update};
        \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (0,\ploth+0.25)
          node[above, font=\scriptsize] {evaluation return};

        \draw[successgreen, dashed, thick] (0,\threshold) -- (\plotw,\threshold);
        \node[anchor=south east, font=\scriptsize, text=successgreen]
          at (\plotw,\threshold) {solved threshold};

        \input{assets/eval_return_1m_tikz.tex}

        \draw[tuwblue!28, line width=1pt] (0.25,4.55) -- (0.65,4.55);
        \node[anchor=west, font=\scriptsize] at (0.72,4.55) {periodic eval.};
        \draw[tuwblue, very thick] (0.25,4.30) -- (0.65,4.30);
        \node[anchor=west, font=\scriptsize] at (0.72,4.30) {moving average};
      \end{tikzpicture}
    \end{column}
    \begin{column}{0.30\textwidth}
      \small
      \textbf{1M-timestep ablation run}\\[0.4em]
      \begin{itemize}
        \item Same selected configuration as the Makefile ablation
        \item Periodic stochastic evaluation during training
        \item Final $60$ episodes: $500.0 \pm 0.0$, $100\%$ success
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Results: Loss}
  \centering
  \begin{tikzpicture}[x=1cm,y=1cm]
    \def\plotw{11.1}
    \def\ploth{5.15}

    \foreach \x/\lab in {0/0,3.56/5k,7.11/10k,10.67/15k} {
      \draw[tuwgray!18] (\x,0) -- (\x,\ploth);
      \node[font=\scriptsize, below] at (\x,0) {\lab};
    }
    \foreach \y/\lab in {0/0,1.72/20,3.43/40,5.15/60} {
      \draw[tuwgray!18] (0,\y) -- (\plotw,\y);
      \node[font=\scriptsize, left] at (0,\y) {\lab};
    }

    \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (\plotw+0.25,0)
      node[below, font=\scriptsize] {optimizer step};
    \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (0,\ploth+0.25)
      node[above, font=\scriptsize] {loss};

    \input{assets/losses_1m_tikz.tex}
  \end{tikzpicture}
\end{frame}

\end{document}
