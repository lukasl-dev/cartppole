\documentclass{article}

% if you need to pass options to natbib, use, e.g.:
%     \PassOptionsToPackage{numbers, compress}{natbib}
% before loading neurips_2026

% The authors should use one of these tracks.
% Before accepting by the NeurIPS conference, select one of the options below.
% 0. "default" for submission
% \usepackage{neurips_2026}
% the "default" option is equal to the "main" option, which is used for the Main Track with double-blind reviewing.
% 1. "main" option is used for the Main Track
%  \usepackage[main]{neurips_2026}
% 2. "position" option is used for the Position Paper Track
%  \usepackage[position]{neurips_2026}
% 3. "eandd" option is used for the Evaluations & Datasets Track
 % \usepackage[eandd]{neurips_2026}
% 4. "creativeai" option is used for the Creative AI Track
%  \usepackage[creativeai]{neurips_2026}
% 5. "sglblindworkshop" option is used for the Workshop with single-blind reviewing
 % \usepackage[sglblindworkshop]{neurips_2026}
% 6. "dblblindworkshop" option is used for the Workshop with double-blind reviewing
%  \usepackage[dblblindworkshop]{neurips_2026}

% After being accepted, the authors should add "final" behind the track to compile a camera-ready version.
% 1. Main Track
 % \usepackage[main, final]{neurips_2026}
% 2. Position Paper Track
%  \usepackage[position, final]{neurips_2026}
% 3. Evaluations & Datasets Track
 % \usepackage[eandd, final]{neurips_2026}
% 4. Creative AI Track
%  \usepackage[creativeai, final]{neurips_2026}
% 5. Workshop with single-blind reviewing
%  \usepackage[sglblindworkshop, final]{neurips_2026}
% 6. Workshop with double-blind reviewing
%  \usepackage[dblblindworkshop, final]{neurips_2026}
% Note. For the workshop paper template, both \title{} and \workshoptitle{} are required, with the former indicating the paper title shown in the title and the latter indicating the workshop title displayed in the footnote.
% For workshops (5., 6.), the authors should add the name of the workshop, "\workshoptitle" command is used to set the workshop title.
% \workshoptitle{WORKSHOP TITLE}

% "preprint" option is used for arXiv or other preprint submissions
 \usepackage[preprint]{neurips_2026}

% to avoid loading the natbib package, add option nonatbib:
%    \usepackage[nonatbib]{neurips_2026}

\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
\usepackage{hyperref}       % hyperlinks
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{amsmath}        % math environments
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{float}          % force figure placement with [H]
\usepackage{xcolor}         % colors

\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc}

\definecolor{tuwblue}{RGB}{0,102,153}
\definecolor{tuwgray}{RGB}{128,128,128}
\definecolor{successgreen}{RGB}{34,139,34}
\definecolor{failred}{RGB}{178,34,34}
\definecolor{valueorange}{RGB}{230,126,34}
\definecolor{policyviolet}{RGB}{117,80,123}

% Note. For the workshop paper template, both \title{} and \workshoptitle{} are required, with the former indicating the paper title shown in the title and the latter indicating the workshop title displayed in the footnote. 
\title{cartPPOle: \\ Proximal Policy Optimisation for CartPole-v1}


% The \author macro works with any number of authors. There are two commands
% used to separate the names and addresses of multiple authors: \And and \AND.
%
% Using \And between authors leaves it to LaTeX to determine where to break the
% lines. Using \AND forces a line break at that point. So, if LaTeX puts 3 of 4
% authors names on the first line, and the last on the second line, try using
% \AND instead of \And before the third author name.


\author{%
  Lukas Leeb \\
  Technical University of Vienna \\
  \texttt{s12421919@student.tuwien.ac.at} \\
  \And
  Michael Gail \\
  Technical University of Vienna \\
  \texttt{e12317237@student.tuwien.ac.at} \\
}


\begin{document}


\maketitle


\begin{abstract}
  We implement Proximal Policy Optimisation (PPO) for CartPole-v1 using an actor--critic network, clipped policy updates, value loss, entropy regularisation, and GAE. We evaluate the effect of clip range, advantage estimation, and rollout/update ratio over four random seeds. Longer rollouts and GAE gave the most stable results; the best configuration reached $500.0 \pm 0.0$ return over 60 evaluation episodes with a $100\%$ success rate.
\end{abstract}

\begin{center}
  \url{https://github.com/lukasl-dev/cartppole}
\end{center}

% General instructions, not in rendered result
\iffalse

\section{Submission of papers to NeurIPS 2026}


Please read the instructions below carefully and follow them faithfully.


\subsection{Style}


Papers to be submitted to NeurIPS 2026 must be prepared according to the
instructions presented here. Papers may only be up to {\bf nine} pages long, including figures. \textbf{Papers that exceed the page limit will not be reviewed (or in any other way considered) for presentation at the conference.}
Additional pages \emph{containing acknowledgments, references, checklist, and optional technical appendices} do not count as content pages. 


The margins in 2026 are the same as those in previous years.


Authors are required to use the NeurIPS \LaTeX{} style files obtainable at the NeurIPS website as indicated below. Please make sure you use the current files and not previous versions. Tweaking the style files may be grounds for desk rejection.


\subsection{Retrieval of style files}


The style files for NeurIPS and other conference information are available on the website at
\begin{center}
  \url{https://neurips.cc}.
\end{center}
% The file \verb+neurips_2026.pdf+ contains these instructions and illustrates the various formatting requirements your NeurIPS paper must satisfy.


The only supported style file for NeurIPS 2026 is \verb+neurips_2026.sty+, rewritten for \LaTeXe{}. \textbf{Previous style files for \LaTeX{} 2.09,  Microsoft Word, and RTF are no longer supported.}


The \LaTeX{} style file contains three optional arguments: 
\begin{itemize}
\item \verb+final+, which creates a camera-ready copy, 
\item \verb+preprint+, which creates a preprint for submission to, e.g., arXiv, \item \verb+nonatbib+, which will not load the \verb+natbib+ package for you in case of package clash.
\end{itemize}


\paragraph{Preprint option}
If you wish to post a preprint of your work online, e.g., on arXiv, using the NeurIPS style, please use the \verb+preprint+ option. This will create a nonanonymized version of your work with the text ``Preprint. Work in progress.'' in the footer. This version may be distributed as you see fit, as long as you do not say which conference it was submitted to. Please \textbf{do not} use the \verb+final+ option, which should \textbf{only} be used for papers accepted to NeurIPS.


At submission time, please omit the \verb+final+ and \verb+preprint+ options. This will anonymize your submission and add line numbers to aid review. Please do \emph{not} refer to these line numbers in your paper as they will be removed during generation of camera-ready copies.


The file \verb+neurips_2026.tex+ may be used as a ``shell'' for writing your paper. All you have to do is replace the author, title, abstract, and text of the paper with your own.


The formatting instructions contained in these style files are summarized in Sections \ref{gen_inst}, \ref{headings}, and \ref{others} below.


\section{General formatting instructions}
\label{gen_inst}


The text must be confined within a rectangle 5.5~inches (33~picas) wide and
9~inches (54~picas) long. The left margin is 1.5~inch (9~picas).  Use 10~point
type with a vertical spacing (leading) of 11~points.  Times New Roman is the
preferred typeface throughout, and will be selected for you by default.
Paragraphs are separated by \nicefrac{1}{2}~line space (5.5 points), with no
indentation.


The paper title should be 17~point, initial caps/lower case, bold, centered
between two horizontal rules. The top rule should be 4~points thick and the
bottom rule should be 1~point thick. Allow \nicefrac{1}{4}~inch space above and
below the title to rules. All pages should start at 1~inch (6~picas) from the
top of the page.


For the final version, authors' names are set in boldface, and each name is
centered above the corresponding address. The lead author's name is to be listed
first (left-most), and the co-authors' names (if different address) are set to
follow. If there is only one co-author, list both author and co-author side by
side.


Please pay special attention to the instructions in Section \ref{others}
regarding figures, tables, acknowledgments, and references.

\section{Headings: first level}
\label{headings}


All headings should be lower case (except for first word and proper nouns),
flush left, and bold.


First-level headings should be in 12-point type.


\subsection{Headings: second level}


Second-level headings should be in 10-point type.


\subsubsection{Headings: third level}


Third-level headings should be in 10-point type.


\paragraph{Paragraphs}


There is also a \verb+\paragraph+ command available, which sets the heading in
bold, flush left, and inline with the text, with the heading followed by 1\,em
of space.


\section{Citations, figures, tables, references}
\label{others}


These instructions apply to everyone.


\subsection{Citations within the text}


The \verb+natbib+ package will be loaded for you by default.  Citations may be
author/year or numeric, as long as you maintain internal consistency.  As to the
format of the references themselves, any style is acceptable as long as it is
used consistently.


The documentation for \verb+natbib+ may be found at
\begin{center}
  \url{http://mirrors.ctan.org/macros/latex/contrib/natbib/natnotes.pdf}
\end{center}
Of note is the command \verb+\citet+, which produces citations appropriate for
use in inline text.  For example,
\begin{verbatim}
   \citet{hasselmo} investigated\dots
\end{verbatim}
produces
\begin{quote}
  Hasselmo, et al.\ (1995) investigated\dots
\end{quote}


If you wish to load the \verb+natbib+ package with options, you may add the
following before loading the \verb+neurips_2026+ package:
\begin{verbatim}
   \PassOptionsToPackage{options}{natbib}
\end{verbatim}


If \verb+natbib+ clashes with another package you load, you can add the optional
argument \verb+nonatbib+ when loading the style file:
\begin{verbatim}
   \usepackage[nonatbib]{neurips_2026}
\end{verbatim}


As submission is double blind, refer to your own published work in the third
person. That is, use ``In the previous work of Jones et al.\ [4],'' not ``In our
previous work [4].'' If you cite your other papers that are not widely available
(e.g., a journal paper under review), use anonymous author names in the
citation, e.g., an author of the form ``A.\ Anonymous'' and include a copy of the anonymized paper in the supplementary material.


\subsection{Footnotes}


Footnotes should be used sparingly.  If you do require a footnote, indicate
footnotes with a number\footnote{Sample of the first footnote.} in the
text. Place the footnotes at the bottom of the page on which they appear.
Precede the footnote with a horizontal rule of 2~inches (12~picas).


Note that footnotes are properly typeset \emph{after} punctuation
marks.\footnote{As in this example.}


\subsection{Figures}


\begin{figure}[H]
  \centering
  \fbox{\rule[-.5cm]{0cm}{4cm} \rule[-.5cm]{4cm}{0cm}}
  \caption{Sample figure caption. Explain what the figure shows and add a key take-away message to the caption.}
\end{figure}


All artwork must be neat, clean, and legible. Lines should be dark enough for
 reproduction purposes. The figure number and caption always appear after the
figure. Place one line space before the figure caption and one line space after
the figure. The figure caption should be lower case (except for the first word and proper nouns); figures are numbered consecutively.


You may use color figures.  However, it is best for the figure captions and the
paper body to be legible if the paper is printed in either black/white or in
color.


\subsection{Tables}


All tables must be centered, neat, clean, and legible.  The table number and
title always appear before the table.  See Table~\ref{sample-table}.


Place one line space before the table title, one line space after the
table title, and one line space after the table. The table title must
be lower case (except for the first word and proper nouns); tables are
numbered consecutively.


Note that publication-quality tables \emph{do not contain vertical rules}. We
strongly suggest the use of the \verb+booktabs+ package, which allows for
typesetting high-quality, professional tables:
\begin{center}
  \url{https://www.ctan.org/pkg/booktabs}
\end{center}
This package was used to typeset Table~\ref{sample-table}.


\begin{table}
  \caption{Sample table caption. Explain what the table shows and add a key take-away message to the caption.}
  \label{sample-table}
  \centering
  \begin{tabular}{lll}
    \toprule
    \multicolumn{2}{c}{Part}                   \\
    \cmidrule(r){1-2}
    Name     & Description     & Size ($\mu$m) \\
    \midrule
    Dendrite & Input terminal  & $\approx$100     \\
    Axon     & Output terminal & $\approx$10      \\
    Soma     & Cell body       & up to $10^6$  \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Math}
Note that display math in bare TeX commands will not create correct line numbers for submission. Please use LaTeX (or AMSTeX) commands for unnumbered display math. (You really shouldn't be using \$\$ anyway; see \url{https://tex.stackexchange.com/questions/503/why-is-preferable-to} and \url{https://tex.stackexchange.com/questions/40492/what-are-the-differences-between-align-equation-and-displaymath} for more information.)

\subsection{Final instructions}

Do not change any aspects of the formatting parameters in the style files.  In
particular, do not modify the width or length of the rectangle the text should
fit into, and do not change font sizes. Please note that pages should be
numbered.


\section{Preparing PDF files}


Please prepare submission files with paper size ``US Letter,'' and not, for
example, ``A4.''


Fonts were the main cause of problems in the past years. Your PDF file must only
contain Type 1 or Embedded TrueType fonts. Here are a few instructions to
achieve this.


\begin{itemize}


\item You should directly generate PDF files using \verb+pdflatex+.


\item You can check which fonts a PDF files uses.  In Acrobat Reader, select the
  menu Files$>$Document Properties$>$Fonts and select Show All Fonts. You can
  also use the program \verb+pdffonts+ which comes with \verb+xpdf+ and is
  available out-of-the-box on most Linux machines.


\item \verb+xfig+ ``patterned'' shapes are implemented with bitmap fonts.  Use
  "solid" shapes instead.


\item The \verb+\bbold+ package almost always uses bitmap fonts.  You should use
  the equivalent AMS Fonts:
\begin{verbatim}
   \usepackage{amsfonts}
\end{verbatim}
followed by, e.g., \verb+\mathbb{R}+, \verb+\mathbb{N}+, or \verb+\mathbb{C}+
for $\mathbb{R}$, $\mathbb{N}$ or $\mathbb{C}$.  You can also use the following
workaround for reals, natural and complex:
\begin{verbatim}
   \newcommand{\RR}{I\!\!R} %real numbers
   \newcommand{\Nat}{I\!\!N} %natural numbers
   \newcommand{\CC}{I\!\!\!\!C} %complex numbers
\end{verbatim}
Note that \verb+amsfonts+ is automatically loaded by the \verb+amssymb+ package.


\end{itemize}


If your file contains type 3 fonts or non embedded TrueType fonts, we will ask
you to fix it.


\subsection{Margins in \LaTeX{}}


Most of the margin problems come from figures positioned by hand using
\verb+\special+ or other commands. We suggest using the command
\verb+\includegraphics+ from the \verb+graphicx+ package. Always specify the
figure width as a multiple of the line width as in the example below:
\begin{verbatim}
   \usepackage[pdftex]{graphicx} ...
   \includegraphics[width=0.8\linewidth]{myfile.pdf}
\end{verbatim}
See Section 4.4 in the graphics bundle documentation
(\url{http://mirrors.ctan.org/macros/latex/required/graphics/grfguide.pdf})


A number of width problems arise when \LaTeX{} cannot properly hyphenate a
line. Please give LaTeX hyphenation hints using the \verb+\-+ command when
necessary.

\begin{ack}
Use unnumbered first level headings for the acknowledgments. All acknowledgments
go at the end of the paper before the list of references. Moreover, you are required to declare
funding (financial activities supporting the submitted work) and competing interests (related financial activities outside the submitted work).
More information about this disclosure can be found at: \url{https://neurips.cc/Conferences/2026/PaperInformation/FundingDisclosure}.


Do {\bf not} include this section in the anonymized submission, only in the final paper. You can use the \texttt{ack} environment provided in the style file to automatically hide this section in the anonymized submission.
\end{ack}

\section*{References}


References follow the acknowledgments in the camera-ready paper. Use unnumbered first-level heading for
the references. Any choice of citation style is acceptable as long as you are
consistent. It is permissible to reduce the font size to \verb+small+ (9 point)
when listing the references.
Note that the Reference section does not count towards the page limit.
\medskip


{
\small


[1] Alexander, J.A.\ \& Mozer, M.C.\ (1995) Template-based algorithms for
connectionist rule extraction. In G.\ Tesauro, D.S.\ Touretzky and T.K.\ Leen
(eds.), {\it Advances in Neural Information Processing Systems 7},
pp.\ 609--616. Cambridge, MA: MIT Press.


[2] Bower, J.M.\ \& Beeman, D.\ (1995) {\it The Book of GENESIS: Exploring
  Realistic Neural Models with the GEneral NEural SImulation System.}  New York:
TELOS/Springer--Verlag.


[3] Hasselmo, M.E., Schnell, E.\ \& Barkai, E.\ (1995) Dynamics of learning and
recall at excitatory recurrent synapses and cholinergic modulation in rat
hippocampal region CA3. {\it Journal of Neuroscience} {\bf 15}(7):5249-5262.
}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\appendix

\section{Technical appendices and supplementary material}
Technical appendices with additional results, figures, graphs, and proofs may be submitted with the paper submission before the full submission deadline (see above). You can upload a ZIP file for videos or code, but do not upload a separate PDF file for the appendix. There is no page limit for the technical appendices. 

Note: Think of the appendix as ``optional reading'' for reviewers. The paper must be able to stand alone without the appendix; for example, adding critical experiments that support the main claims to an appendix is inappropriate. 

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% end of general instructions
\fi

\section{Introduction and Background}

Reinforcement Learning is an approach to artificial intelligence that works without fixed input-output ground truths~\citep{sutton2018rl}. Instead, an agent can learn to make decisions by continuously interacting with an environment.
In this loop an agent observes a state from the environment, based on which it chooses its next action. This leads to the environment transitioning into a new state and responding to the agent's action
with a reward indicating its value.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      node distance=2.5cm,
      box/.style={rectangle, draw=black!80, thick, minimum width=2.8cm, minimum height=1.2cm, rounded corners},
      arrow/.style={-{Latex[length=2mm]}, thick, black!80}
    ]
    \node[box] (agent) {\textbf{Agent}};
    \node[box, right=of agent] (env) {\textbf{Environment}};
    \draw[arrow, bend left=40] (agent.north) to node[above] {action} (env.north);
    \draw[arrow, bend left=40] (env.south) to node[below] {state, reward} (agent.south);
  \end{tikzpicture}
  \caption{The reinforcement learning loop: the agent chooses an action, and the environment responds with a new state and a reward.}
\end{figure}

This reward signal can be used to optimize the agent's policy for taking actions, enabling the agent to learn.

In this project, PPO was implemented from scratch to solve the CartPole-v1 environment. Beyond training a working agent, the goal is also to study how choices of different parameters affect training stability and final performance,
such as the clipping range, the advantage estimator (Monte Carlo returns versus Generalized Advantage Estimation with varying \(\lambda\)) and the rollout length/update epochs ratio.

Each configuration is tested across multiple seeds and evaluated on a fixed, reproducible set of episodes, allowing conclusions about performance and stability to be drawn.

\subsection{Environment}

Formally, an environment is modeled as a Markov Decision Process (MDP). At each discrete time step \(t = 0, 1, 2, \dots\), the environment is in a state \(S_t\), the agent selects an action \(A_t\), and (for \(t \neq 0\)) receives a reward \(R_t\). The environment then transitions into the next state, where the resulting state and reward are governed by a transition probability distribution \(p(s' \mid s, a)\) and a reward function \(r(s,a)\).

CartPole-v1\footnote{Gymnasium: \url{https://gymnasium.farama.org/environments/classic_control/cart_pole/}} models a pole attached to a cart moving along a frictionless track. The goal is to keep the pole balanced by applying a force to the cart, either to the left or to the right.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.3\textwidth]{CartPole.png}
  \caption{The CartPole-v1 environment: a pole attached to a cart moving along a frictionless track.}
\end{figure}

The environment specification is compactly summarised below:

\begingroup
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{0.92}
\noindent
\begin{tabular}{@{}p{0.24\textwidth}p{0.68\textwidth}@{}}
\toprule
\textbf{Action space} & \(0\) push left, \(1\) push right \\
\textbf{Observation space} & cart position \([-4.8,4.8]\), cart velocity \((-\infty,\infty)\), pole angle \([-24^\circ,24^\circ]\), pole angular velocity \((-\infty,\infty)\) \\
\textbf{Reward} & \(1\) per non-terminal time step \\
\textbf{Termination} & pole angle exceeds \(\pm12^\circ\), or cart leaves \([-2.4,2.4]\) \\
\bottomrule
\end{tabular}
\endgroup

\section{Proximal Policy Optimization}

PPO~\citep{schulman2017ppo} is an on-policy actor--critic method. It alternates between \emph{sampling} with the current policy and \emph{optimizing} a surrogate objective on the collected data. The \textbf{actor} $\pi_\theta(a \mid s)$ chooses actions and is the policy being improved; the \textbf{critic} $V_\phi(s)$ estimates the state value and is used to compute advantages $\hat{A}_t$. Each rollout is reused for several mini-batch epochs, but the clipped objective keeps the new policy close to the one that collected the data.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      scale=0.85,
      transform shape,
      box/.style={rectangle, draw=tuwblue!80, thick, rounded corners,
                  minimum height=0.75cm,
                  align=center, font=\small},
      smallbox/.style={rectangle, draw=tuwblue!60, thick, rounded corners,
                       minimum height=0.65cm,
                       align=center, font=\scriptsize},
      leftbox/.style={box, text width=2.75cm},
      rightbox/.style={box, text width=3.05cm},
      leftsmall/.style={smallbox, text width=2.75cm},
      rightsmall/.style={smallbox, text width=3.05cm},
      arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!70},
      node distance=0.55cm and 1.35cm
    ]
    \node[leftbox] (actor) {Actor\\$\pi_{\theta_{\mathrm{old}}}(a\mid s)$};
    \node[rightbox, right=of actor] (env) {Environment\\CartPole};
    \node[rightsmall, below=of env] (rollout) {rollout data\\$s_t,a_t,r_t$\\$\log\pi_{\theta_{\mathrm{old}}}$};
    \node[leftbox, below=of actor] (critic) {Critic\\$V_\phi(s)$};
    \node[rightsmall, below=of rollout] (adv) {advantage\\estimates $\hat{A}_t$};
    \node[leftsmall] (update) at (critic |- adv) {PPO update\\clipped surrogate};

    \draw[arrow] (actor.east) -- node[midway, above=2pt, font=\scriptsize] {actions} (env.west);
    \draw[arrow] (env.south) -- node[right, font=\scriptsize] {samples} (rollout.north);
    \draw[arrow] (rollout.south) -- (adv.north);
    \draw[arrow] (critic.east) -- node[midway, above=3pt, xshift=0.12cm, font=\scriptsize] {$V(s_t)$} (adv.west);
    \draw[arrow] (adv.west) -- (update.east);
    \draw[arrow] (update.north) -- node[left, font=\scriptsize] {fit $V$} (critic.south);
    \draw[arrow, rounded corners] (update.west) -- ++(-0.45,0) |- node[left, font=\scriptsize] {$\theta\leftarrow$} (actor.west);
  \end{tikzpicture}
  \caption{PPO overview. The actor collects a rollout, the critic helps compute advantages, and the clipped surrogate objective updates both networks before the next rollout.}
  \label{fig:ppo-overview}
\end{figure}

PPO does not search over actions directly; it changes the network parameters so that the policy distribution assigns better probabilities. The PPO paper writes objectives to maximize; in our implementation we minimise the negative objective with Adam:
\[
  \theta,\phi \leftarrow \theta,\phi - \alpha\,\nabla_{\theta,\phi}\mathcal{L}^{\mathrm{PPO}}.
\]

\subsection{Objective}

\subsubsection{Clipped policy objective}

The actor is updated with the clipped surrogate objective. Let $r_t(\theta)$ be the probability ratio between the new and the old policy on the sampled action:
\[
  r_t(\theta) = \frac{\pi_\theta(a_t\mid s_t)}{\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)}, \qquad r_t(\theta_{\mathrm{old}}) = 1.
\]
The clipped objective is then
\[
  L_t^{\mathrm{CLIP}}(\theta) = \min\!\left( r_t(\theta)\hat{A}_t,\; \operatorname{clip}\bigl(r_t(\theta), 1-\epsilon, 1+\epsilon\bigr)\hat{A}_t \right).
\]
The ratio compares the new policy to the policy that collected the rollout, $\hat{A}_t$ signals whether the sampled action was better or worse than expected, and the clipping removes the incentive for overly large policy changes. ``Proximal'' means the update is restricted: PPO removes the incentive for probability ratios outside $[1-\epsilon, 1+\epsilon]$.

\subsubsection{Value-function loss}

The critic is trained as a regression problem, predicting the return target for each sampled state:
\[
  L_t^{\mathrm{VF}}(\phi) = \bigl(V_\phi(s_t) - V_t^{\mathrm{target}}\bigr)^2.
\]
A better critic yields better advantage estimates and reduces update variance.

\subsubsection{Entropy bonus and combined objective}

PPO also rewards stochastic policies to avoid collapsing to one action too early. The entropy bonus is
\[
  S[\pi_\theta](s_t) = -\sum_a \pi_\theta(a\mid s_t)\log \pi_\theta(a\mid s_t),
\]
and the combined objective minimised in our implementation is the negative of
\[
  L_t^{\mathrm{PPO}}(\theta, \phi) = L_t^{\mathrm{CLIP}}(\theta) - c_1 L_t^{\mathrm{VF}}(\phi) + c_2 S[\pi_\theta](s_t),
\]
with value coefficient $c_1$ and entropy coefficient $c_2$.

\subsection{Advantage estimation}

The policy loss needs a signal for whether the sampled action was good. Instead of using raw return, PPO uses an \textbf{advantage estimate}, which compares an estimate of the action's return with the critic's baseline:
\[
  \hat{A}_t \approx \underbrace{\widehat{Q}_t}_{\text{rollout estimate}} - \underbrace{V_\phi(s_t)}_{\text{critic baseline}}.
\]
If $\hat{A}_t > 0$ the action was better than expected and is made more likely; if $\hat{A}_t < 0$ it is made less likely.

\subsubsection{Rollout return}

A direct estimate compares a discounted rollout return with the critic's current value estimate. For a rollout segment of length $T$ starting at position $t$,
\[
  G_t = \sum_{l=0}^{T-t-1} \gamma^l r_{t+l} + \gamma^{T-t} V_\phi(s_T), \qquad \hat{A}_t = G_t - V_\phi(s_t).
\]
This uses the real rewards from the sampled trajectory segment and \emph{bootstraps} with $V_\phi(s_T)$ because PPO uses fixed-length rollouts that may stop before the episode ends. The unobserved future after $s_T$ is replaced by the critic's estimate; if $s_T$ is terminal, that estimate is $0$. This estimate has low bias but can have high variance.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      state/.style={circle, draw=tuwblue!80, thick, minimum size=0.72cm, font=\scriptsize},
      reward/.style={rectangle, draw=tuwblue!60, rounded corners, minimum width=0.65cm,
                     minimum height=0.45cm, font=\scriptsize},
      arrow/.style={-{Latex[length=1.6mm]}, thick, tuwblue!70},
      node distance=0.45cm
    ]
    \node[state] (st) {$s_t$};
    \node[reward, right=of st] (rt) {$r_t$};
    \node[state, right=of rt] (st1) {$s_{t+1}$};
    \node[right=0.25cm of st1] (dots) {$\cdots$};
    \node[reward, right=0.25cm of dots] (rT) {$r_{T-1}$};
    \node[state, right=of rT] (sT) {$s_T$};
    \node[reward, right=0.85cm of sT, dashed, draw=failred!70] (future) {$?$};
    \node[below=0.25cm of future, font=\scriptsize, text=failred] {unobserved future};
    \node[reward, below=0.72cm of sT, draw=successgreen!80, text width=1.35cm,
          align=center] (boot) {$V_\phi(s_T)$};

    \draw[arrow] (st) -- (rt);
    \draw[arrow] (rt) -- (st1);
    \draw[arrow] (st1) -- (dots);
    \draw[arrow] (dots) -- (rT);
    \draw[arrow] (rT) -- (sT);
    \draw[arrow, dashed, failred!70] (sT) -- (future);
    \draw[arrow, successgreen!80] (sT) -- (boot);
  \end{tikzpicture}
  \caption{Bootstrapping in a fixed-length rollout. The unobserved future after $s_T$ is replaced by the critic's value estimate $V_\phi(s_T)$.}
  \label{fig:bootstrapping}
\end{figure}

\subsubsection{Generalized Advantage Estimation}

Generalized Advantage Estimation (GAE) smooths many temporal-difference (TD) errors into one advantage estimate:
\[
  \delta_t = r_t + \gamma V(s_{t+1}) - V(s_t), \qquad \hat{A}_t^{\mathrm{GAE}} = \sum_{l=0}^{T-t-1} (\gamma\lambda)^l \delta_{t+l}.
\]
Here $\delta_t$ measures the one-step surprise relative to the critic, and $\lambda$ controls how far future TD errors influence $\hat{A}_t$. The critic target used later is $V_t^{\mathrm{target}} = \hat{A}_t + V(s_t)$. Expanding the sum,
\[
  \hat{A}_t^{\mathrm{GAE}} = \delta_t + (\gamma\lambda)\delta_{t+1} + (\gamma\lambda)^2\delta_{t+2} + \cdots,
\]
so small $\lambda$ relies more on the critic (lower variance, more bias) while large $\lambda$ approaches the Monte-Carlo return (lower bias, more variance). $\lambda=0$ gives the one-step TD estimate and $\lambda=1$ the rollout-return-like estimate.

\subsection{Clipped vs.\ unclipped objective}

PPO reuses the same rollout for several gradient steps, so the ratio $r_t(\theta)$ measures how much the new policy changed on the sampled action. The unclipped surrogate $L_t^{\mathrm{unclip}}(\theta)=r_t(\theta)\hat{A}_t$ would push the policy as far as the gradient allows: nothing stops very large policy changes. The clipped surrogate
\[
  L_t^{\mathrm{CLIP}}(\theta) = \min\!\left( r_t\hat{A}_t,\; \operatorname{clip}(r_t, 1-\epsilon, 1+\epsilon)\hat{A}_t \right)
\]
allows updates near the old policy but removes the incentive outside $[1-\epsilon, 1+\epsilon]$, with $\epsilon$ controlling how conservative the update is.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      scale=0.9,
      arrow/.style={-{Latex[length=1.8mm]}, thick, tuwblue!70},
      tick/.style={thick, tuwblue!70},
      label/.style={font=\scriptsize}
    ]
    \draw[arrow] (0,0) -- (7.0,0) node[right, label] {$r_t$};
    \draw[tick] (2,0.12) -- (2,-0.12) node[below, label] {$1-\epsilon$};
    \draw[tick] (3.5,0.12) -- (3.5,-0.12) node[below, label] {$1$};
    \draw[tick] (5,0.12) -- (5,-0.12) node[below, label] {$1+\epsilon$};
    \draw[very thick, successgreen!75] (2,0.28) -- (5,0.28);
    \node[label, successgreen!60!black] at (3.5,0.62) {trusted update region};
    \node[label, failred] at (1.0,0.28) {too far};
    \node[label, failred] at (6.0,0.28) {too far};
  \end{tikzpicture}
  \caption{The clipped objective only rewards updates whose probability ratio stays within $[1-\epsilon,1+\epsilon]$ of the old policy.}
  \label{fig:clip-region}
\end{figure}

\subsection{Training loop}

\subsubsection{One PPO iteration}

PPO alternates between collecting fresh on-policy data and reusing that data for a limited number of optimization steps. The data is \emph{on-policy}: collected with the old policy before the update, and the stored old log-probabilities define the ratio $r_t(\theta)$ during optimization.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      box/.style={rectangle, draw=tuwblue!80, thick, rounded corners,
                  minimum width=3.0cm, minimum height=0.9cm, align=center},
      arrow/.style={-{Latex[length=2mm]}, thick, tuwblue!70},
      node distance=0.7cm
    ]
    \node[box] (old) {current policy\\$\pi_{\theta_{\mathrm{old}}}$};
    \node[box, right=of old] (rollout) {collect rollout\\$N\times T$ samples};
    \node[box, right=of rollout] (adv) {compute\\$\hat{A}_t, V_t^{\mathrm{target}}$};
    \node[box, below=of adv] (update) {mini-batch updates\\$K$ epochs};
    \node[box, left=of update] (new) {new policy\\$\pi_\theta$};

    \draw[arrow] (old) -- (rollout);
    \draw[arrow] (rollout) -- (adv);
    \draw[arrow] (adv) -- (update);
    \draw[arrow] (update) -- (new);
    \draw[arrow]
      (new.west) .. controls +(-1.0,0) and +(0,-1.0) ..
      node[pos=0.55, xshift=-0.2cm, left, font=\scriptsize] {$\theta_{\mathrm{old}}\leftarrow\theta$}
      (old.south);
  \end{tikzpicture}
  \caption{One PPO iteration: collect a rollout, compute advantages and targets, perform $K$ epochs of mini-batch updates, then copy the new weights into the old policy.}
  \label{fig:ppo-iteration}
\end{figure}

\subsubsection{Parallel rollout collection}

Instead of one environment, PPO commonly runs $N$ environments in parallel for $T$ steps, giving a batch of $N\cdot T$ transitions per iteration. For each transition PPO stores $s_t, a_t, r_t, \text{done}_t, \log\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)$, and $V_\phi(s_t)$.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      cell/.style={rectangle, draw=tuwblue!65, rounded corners,
                   minimum width=1.05cm, minimum height=0.5cm, font=\scriptsize},
      label/.style={font=\scriptsize},
      arrow/.style={-{Latex[length=1.6mm]}, thick, tuwblue!70}
    ]
    \foreach \row/\env in {0/1,1/2,2/\vdots,3/N} {
      \node[label] at (-1.05,-\row*0.7) {env $\env$};
      \node[cell] at (0,-\row*0.7) {$t$};
      \node[cell] at (1.35,-\row*0.7) {$t+1$};
      \node[cell] at (2.70,-\row*0.7) {$\cdots$};
      \node[cell] at (4.35,-\row*0.7) {$t+T-1$};
    }
    \draw[arrow] (-0.1,0.55) -- (4.65,0.55) node[right,label] {$T$ steps};
    \draw[arrow] (5.45,0.05) -- (5.45,-2.15) node[below, xshift=0.25cm, label] {$N$ envs};
  \end{tikzpicture}
  \caption{Parallel rollout collection: $N$ environments stepped for $T$ steps yield $N\cdot T$ transitions per iteration.}
  \label{fig:parallel-rollout}
\end{figure}

\subsubsection{From rollout to training batch}

After rollout collection, the data is converted into supervised-style targets for the actor and critic. From the rewards we bootstrap from $V_\phi(s_T)$ if needed, compute advantages $\hat{A}_t$ using the Monte-Carlo return or GAE, and form the critic targets $V_t^{\mathrm{target}} = \hat{A}_t + V_\phi(s_t)$. The rollout of shape $N\times T$ is then flattened into a training batch of size $N\cdot T$, shuffled, split into mini-batches, and the advantages are optionally normalized before updates.

\subsubsection{Mini-batch updates}

PPO does not update once on the whole rollout. It shuffles the rollout batch, splits it into mini-batches, and performs several passes over the same data. For each epoch and mini-batch, the networks recompute $\log\pi_\theta(a_t\mid s_t)$ and $V_\phi(s_t)$ with the current parameters, form the ratio
\[
  r_t(\theta) = \exp\!\bigl(\log\pi_\theta(a_t\mid s_t) - \log\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)\bigr),
\]
and optimize the policy loss, value loss, and entropy bonus with Adam. Reusing data improves sample efficiency, and clipping makes this reuse safer by limiting policy drift from $\pi_{\theta_{\mathrm{old}}}$. After all updates, $\theta_{\mathrm{old}} \leftarrow \theta$ and a new rollout is collected.

\section{Methodology}
\label{sec:methodology}

For the purposes of this paper, the PPO algorithm was implemented using PyTorch\footnote{\url{https://pytorch.org/}} as a configurable training loop, allowing many variations of the algorithm to be trained via command-line parameters. Evaluation was implemented as a separate step, loading a trained checkpoint and rolling it out on a fixed, reproducible set of CartPole-v1 episodes. Both were combined in a sweep script, which trains and evaluates one agent for every combination of a chosen set of hyperparameters and random seeds.

\subsection{Network architecture}

A single network architecture was tested for both the actor and critic network. Both networks were constructed as MLPs with 2 hidden layers using \(\tanh\) activation functions.

\begin{figure}[H]
  \centering
  \begin{minipage}{0.45\textwidth}
    \centering
    \textbf{Actor} $\pi_\theta(a \mid s)$\\[0.5em]
    \begin{tikzpicture}[
        layer/.style={rectangle, draw=black!80, thick, minimum width=2.6cm,
                      minimum height=0.65cm, rounded corners, font=\small},
        arrow/.style={-{Latex[length=2mm]}, thick, black!60},
        node distance=0.45cm
      ]
      \node[layer] (in)  {Input $(4)$};
      \node[layer, below=of in]  (h1) {Linear$(64)$ + Tanh};
      \node[layer, below=of h1]  (h2) {Linear$(64)$ + Tanh};
      \node[layer, below=of h2]  (out) {Linear$(2)$};
      \node[below=0.35cm of out, font=\small, text=tuwgray] (dist) {Categorical$(logits)$};
      \draw[arrow] (in) -- (h1);
      \draw[arrow] (h1) -- (h2);
      \draw[arrow] (h2) -- (out);
      \draw[arrow] (out) -- (dist);
    \end{tikzpicture}
  \end{minipage}%
  \hfill
  \begin{minipage}{0.45\textwidth}
    \centering
    \textbf{Critic} $V_\phi(s)$\\[0.5em]
    \begin{tikzpicture}[
        layer/.style={rectangle, draw=black!80, thick, minimum width=2.6cm,
                      minimum height=0.65cm, rounded corners, font=\small},
        arrow/.style={-{Latex[length=2mm]}, thick, black!60},
        node distance=0.45cm
      ]
      \node[layer] (in)  {Input $(4)$};
      \node[layer, below=of in]  (h1) {Linear$(64)$ + Tanh};
      \node[layer, below=of h1]  (h2) {Linear$(64)$ + Tanh};
      \node[layer, below=of h2]  (out) {Linear$(1)$};
      \node[below=0.35cm of out, font=\small, text=tuwgray] (val) {$V(s) \in \mathbb{R}$};
      \draw[arrow] (in) -- (h1);
      \draw[arrow] (h1) -- (h2);
      \draw[arrow] (h2) -- (out);
      \draw[arrow] (out) -- (val);
    \end{tikzpicture}
  \end{minipage}
  \caption{Actor and critic network architectures used for CartPole-v1. Both share a two-layer 64-unit MLP with Tanh activations. The actor outputs logits over the two discrete actions, the critic outputs a scalar value estimate.}
\end{figure}

\subsection{Hyperparameter sweep}

The project code allows the most important hyperparameters to be configured. A sweep is done over the product of all configured parameters, doing one training run for every combination of parameters.
During training, metrics of the model's performance were continuously collected. The actual evaluation, however, was done separately after training.

To collect evaluation metrics, a fixed amount of CartPole-v1 episodes was run. For the presented results, this was a total of \(60\) episodes per agent.
The seeds used for the evaluation run were consistent over all configurations to ensure reproducibility.

Furthermore, the number of total steps done in the environment during training was made configurable on a per-sweep basis. The results presented in this paper will use a training budget of 1 million steps.

The following parameters were varied across runs:
\begin{itemize}
  \item \textbf{Clip range} $\epsilon \in \{0.1,\,0.2,\,0.3\}$
  \item \textbf{Advantage estimator} --- Monte Carlo or GAE with $\lambda \in \{0.90,\,0.95,\,0.97\}$
  \item \textbf{Rollout/update ratio} --- (rollout length, epochs) pairs: $64{:}4,\;128{:}4,\;256{:}4,\;128{:}8$
\end{itemize}

All other parameters were kept fixed for all runs, using constants found in Table~\ref{tab:fixed-params}.

\begin{table}[ht]
  \caption{Fixed hyperparameters used across all sweep runs.}
  \label{tab:fixed-params}
  \centering
  \begin{tabular}{ll}
    \toprule
    Parameter & Value \\
    \midrule
    Seeds & $0,1,2,3407$ \\
    Timesteps & $1\mathrm{M}$ \\
    Policy loss & clipped \\
    Learning rate $\alpha$ & $2.5\times10^{-4}$ \\
    Discount factor $\gamma$ & $0.99$ \\
    Value coef.\ $c_1$ & $0.5$ \\
    Entropy coef.\ $c_2$ & $0.01$ \\
    Normalise adv.\ & true \\
    Parallel envs $N$ & $8$ \\
    Mini-batch size & $256$ \\
    Eval episodes & $60$ \\
    Success threshold & $475$ \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Metrics}

\subsubsection{Training metrics}

During each training update, a broader set of metrics was logged to monitor optimization dynamics and policy behavior throughout training. Metrics were tracked using Aim\footnote{\url{https://github.com/aimhubio/aim}}, an open-source experiment tracker, which recorded each value alongside the corresponding update or mini-batch step for later inspection.

\begin{itemize}
  \item \textbf{Episode statistics} --- mean return and length of completed episodes, together with the fraction of episodes solved (return $\geq 475$)
  \item \textbf{Rollout statistics} --- mean and total reward, mean return, mean advantage, and mean value estimate collected per rollout
  \item \textbf{Loss components} --- total loss, policy loss, and value loss, logged for every mini-batch update
  \item \textbf{Policy statistics} --- policy entropy and the mean and standard deviation of the probability ratio $r_t(\theta)$
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{TrainingMetrics1.png}
    \includegraphics[width=0.49\textwidth]{TrainingMetrics2.png}
    \caption{Training metrics logged via Aim. Left: loss curves over training updates. Right: rollout value mean over training updates.}
    \label{fig:training_metrics}
\end{figure}

\subsubsection{Evaluation metrics}

During the evaluation step a smaller set of metrics was collected. These were used to judge the model's performance after a full training run.

\begin{itemize}
  \item \textbf{Mean episode return} --- average reward collected per episode; maximum is $500$ per default
  \item \textbf{Standard deviation} --- standard deviation of the episode returns within the evaluation run, to judge stability across evaluation episodes
  \item \textbf{Success rate} --- episode is defined as ''solved'' if return $\geq 475$
\end{itemize}

\section{Results}

All results in this section come from the 1M-step sweep described in Section~\ref{sec:methodology}: CartPole-v1, four seeds $(0,1,2,3407)$, 60 evaluation episodes per run, and a success threshold of $475$ return. Each table reports mean $\pm$ standard deviation across the seeds.

\subsection{Ablation study}

We ablate three dimensions independently, fixing the others at the best value found. Table~\ref{tab:ablation-clip} varies the clip range, Table~\ref{tab:ablation-adv} the advantage estimator, and Table~\ref{tab:ablation-rollout} the rollout/update ratio.

\begin{table}[ht]
  \caption{Clip range ablation (best row in bold). $\epsilon=0.2$ gives the best trade-off between return and success rate.}
  \label{tab:ablation-clip}
  \centering
  \begin{tabular}{ccc}
    \toprule
    $\epsilon$ & Return & Success \\
    \midrule
    $0.1$ & $351.1 \pm 131.2$ & $0.46 \pm 0.42$ \\
    \textbf{$0.2$} & \textbf{$418.1 \pm 133.3$} & \textbf{$0.73 \pm 0.42$} \\
    $0.3$ & $334.3 \pm 169.0$ & $0.49 \pm 0.49$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[ht]
  \caption{Advantage estimator ablation at a fixed rollout of $128{:}4$. GAE is more reliable than Monte-Carlo returns in this sweep, and $\lambda=0.95$ performs best.}
  \label{tab:ablation-adv}
  \centering
  \begin{tabular}{ccc}
    \toprule
    Estimator & Return & Success \\
    \midrule
    GAE $\lambda=0.90$ & $421.9 \pm 133.7$ & $0.75 \pm 0.43$ \\
    \textbf{GAE $\lambda=0.95$} & \textbf{$418.1 \pm 133.3$} & \textbf{$0.73 \pm 0.42$} \\
    GAE $\lambda=0.97$ & $289.4 \pm 117.3$ & $0.20 \pm 0.35$ \\
    MC & $348.2 \pm 112.9$ & $0.23 \pm 0.28$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[ht]
  \caption{Rollout/update ratio ablation. Longer rollouts solve CartPole far more reliably; $256{:}4$ reaches the maximum return with zero variance across seeds.}
  \label{tab:ablation-rollout}
  \centering
  \begin{tabular}{ccc}
    \toprule
    Steps:epochs & Return & Success \\
    \midrule
    $64{:}4$ & $146.5 \pm 29.1$ & $0.00 \pm 0.00$ \\
    $128{:}4$ & $418.1 \pm 133.3$ & $0.73 \pm 0.42$ \\
    \textbf{$256{:}4$} & \textbf{$500.0 \pm 0.0$} & \textbf{$1.00 \pm 0.00$} \\
    $128{:}8$ & $429.4 \pm 55.7$ & $0.68 \pm 0.25$ \\
    \bottomrule
  \end{tabular}
\end{table}

Read together, the tables suggest that not all three knobs matter equally. The clip range matters least: $\epsilon=0.2$ lands in a stable sweet spot, while $0.1$ and $0.3$ fall off, so the surrogate is sensitive to how much freedom it is given. GAE matters more; once $\lambda$ is tuned to $0.95$ it beats Monte-Carlo returns handily, whereas $0.97$ unravels the run, presumably because the estimate turns noisy enough to destabilise updates. What dominates, though, is the rollout length: $64$-step rollouts never learn, since each update is built from too little trajectory to form a meaningful advantage, whereas $256$ steps solve the environment on every seed. Doubling the epochs at half the length ($128{:}8$) does not rescue the shorter rollout, which says the policy is short on experience, not on gradient steps.

The winning recipe combines conservative clipping, low-variance GAE, and longer rollouts, and solves CartPole-v1 perfectly:

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      scale=0.85,
      transform shape,
      card/.style={rectangle, rounded corners, draw=tuwblue!75, thick,
                   fill=tuwblue!5, minimum width=2.35cm, minimum height=0.95cm,
                   align=center, font=\scriptsize},
      result/.style={rectangle, rounded corners, draw=successgreen!80!black,
                     ultra thick, fill=successgreen!12, minimum width=2.95cm,
                     minimum height=1.15cm, align=center, font=\scriptsize\bfseries},
      plus/.style={font=\Large\bfseries, text=tuwblue},
      arrow/.style={-{Latex[length=2mm]}, ultra thick, successgreen!70!black}
    ]
    \node[card] (clip) at (-4.20,0) {conservative\\clip $\epsilon=0.2$};
    \node[plus] at (-2.775,0) {$+$};
    \node[card] (gae) at (-1.35,0) {low-variance\\GAE $\lambda=0.95$};
    \node[plus] at (0.075,0) {$+$};
    \node[card] (roll) at (1.50,0) {longer rollout\\$256{:}4$};
    \node[result] (res) at (4.85,0) {SOLVED\\$500.0\pm0.0$ return\\$100\%$ success};
    \draw[arrow] (roll.east) -- (res.west);
  \end{tikzpicture}
  \caption{The winning configuration: constrain updates, estimate advantages well, and collect enough context per iteration.}
  \label{fig:winning-recipe}
\end{figure}

\subsection{Sample efficiency}

Figure~\ref{fig:sample-efficiency} compares the best GAE run against a Monte-Carlo run on the same configuration otherwise. The GAE run becomes stably solved after about $0.54$M environment steps, while the MC run never reaches the success threshold within the 1M-step budget. This matches the ablation: GAE's lower-variance advantages let the policy improve steadily, whereas the noisier MC estimate hampers learning.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=1cm,y=1cm,
    labelbox/.style={rounded corners=2pt, inner sep=4pt, align=center,
      font=\small},
    metric/.style={draw=tuwgray!45, fill=tuwgray!5, rounded corners=2pt,
      inner sep=4pt, align=center, font=\scriptsize}
  ]
    \def\barw{8.6}
    \def\barh{0.42}
    \def\stablex{4.64}

    \node[font=\bfseries, anchor=east, text=successgreen!65!black]
      at (-5.25,1.25) {GAE};
    \draw[fill=tuwgray!12, draw=tuwgray!45, rounded corners=2pt]
      (-4.95,1.04) rectangle ++(\barw,\barh);
    \draw[fill=successgreen!35, draw=successgreen!65!black, rounded corners=2pt]
      (-4.95,1.04) rectangle ++(\stablex,\barh);
    \draw[successgreen!65!black, line width=1.2pt]
      (-4.95+\stablex,0.92) -- ++(0,0.70);
    \node[labelbox, draw=successgreen!65!black, fill=successgreen!9, anchor=south]
      at (-4.95+\stablex,1.78) {stable solved\\\textbf{$0.54$M steps}};

    \node[font=\bfseries, anchor=east, text=failred!85!black]
      at (-5.25,0.05) {MC};
    \draw[fill=failred!10, draw=failred!45, rounded corners=2pt]
      (-4.95,-0.16) rectangle ++(\barw,\barh);
    \draw[failred!80!black, line width=1.2pt]
      (3.65,-0.28) -- ++(0,0.70);
    \node[labelbox, draw=failred!80!black, fill=failred!7, anchor=south]
      at (3.65,0.58) {not solved after\\\textbf{$1.0$M steps}};

    \foreach \x/\lab in {-4.95/$0$, -0.65/$0.5$M, 3.65/$1.0$M} {
      \draw[tuwgray!45] (\x,-0.45) -- (\x,-0.34);
      \node[font=\scriptsize, below] at (\x,-0.45) {\lab};
    }
    \node[font=\scriptsize, below] at (-0.65,-0.88) {environment steps};

    \node[metric, draw=successgreen!55!black, fill=successgreen!8]
      at (-3.55,-1.65) {GAE selected run\\\textbf{$500.0\pm0.0$}\\$100\%$ success};
    \node[metric, draw=failred!70!black, fill=failred!6]
      at (2.85,-1.65) {MC selected run\\\textbf{$360.8\pm126.2$}\\$25\%$ success};
  \end{tikzpicture}
  \caption{Sample efficiency of GAE versus Monte-Carlo advantages on the selected configuration. GAE reaches stable solved performance at $0.54$M steps; MC does not solve the task within the budget.}
  \label{fig:sample-efficiency}
\end{figure}

\subsection{Learning progress}

Figure~\ref{fig:learning-progress} shows the evaluation return of the selected configuration over the 1M-step training run. Periodic stochastic evaluation oscillates as the policy improves, while the moving average rises steadily. The run first reaches the solved threshold ($475$) at about $0.44$M steps and is consistently solved from $0.54$M steps onward, plateauing at the maximum return of $500$. The final evaluation over 60 episodes gives $500.0 \pm 0.0$ return and a $100\%$ success rate.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=1cm,y=1cm]
    \def\plotw{8.8}
    \def\ploth{4.8}
    \def\threshold{4.385}
    \def\solvedx{3.90}
    \def\solvedy{4.62}
    \def\stablex{4.76}
    \def\stabley{4.62}

    \foreach \x/\lab in {0/0,2.20/.25M,4.40/.50M,6.60/.75M,8.80/1.0M} {
      \draw[tuwgray!18] (\x,0) -- (\x,\ploth);
      \node[font=\scriptsize, below] at (\x,0) {\lab};
    }
    \foreach \y/\lab in {0/0,0.92/100,1.85/200,2.77/300,3.69/400,4.62/500} {
      \draw[tuwgray!18] (0,\y) -- (\plotw,\y);
      \node[font=\scriptsize, left] at (0,\y) {\lab};
    }

    \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (\plotw+0.25,0)
      node[below, font=\scriptsize] {environment steps};
    \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (0,\ploth+0.25)
      node[above, font=\scriptsize] {evaluation return};

    \draw[successgreen, dashed, thick] (0,\threshold) -- (\plotw,\threshold);
    \node[anchor=north east, font=\scriptsize, text=successgreen, yshift=-0.15em]
      at (\plotw,\threshold) {solved threshold};

    \coordinate (solvedpt) at (\solvedx,\solvedy);
    \draw[successgreen!70!black, dashed, line width=1.2pt]
      (\solvedx,0) -- (solvedpt);
    \coordinate (stablept) at (\stablex,\stabley);
    \draw[successgreen!45!black, dashed, line width=1.2pt]
      (\stablex,0) -- (stablept);

    \input{eval_return_1m_tikz.tex}

    \fill[successgreen!70!black] (solvedpt) circle (3.1pt);
    \fill[white] (solvedpt) circle (1.35pt);
    \node[draw=successgreen!70!black, fill=successgreen!8,
          rounded corners=2pt, inner sep=2.4pt, align=left,
          font=\scriptsize, anchor=south] (solvedlabel)
      at (2.65,5.08) {first solved eval.\\\textbf{$0.44$M steps}};
    \draw[-{Latex[length=1.5mm]}, successgreen!70!black, thick]
      (solvedlabel.south east) -- ($(solvedpt)+(0.08,-0.05)$);

    \fill[successgreen!45!black] (stablept) circle (3.1pt);
    \fill[white] (stablept) circle (1.35pt);
    \node[draw=successgreen!45!black, fill=successgreen!14,
          rounded corners=2pt, inner sep=2.4pt, align=left,
          font=\scriptsize, anchor=south] (stablelabel)
      at (6.20,5.08) {consistently solved\\\textbf{$0.54$M steps}};
    \draw[-{Latex[length=1.5mm]}, successgreen!45!black, thick]
      (stablelabel.south west) -- ($(stablept)+(0.06,-0.07)$);

    \draw[tuwblue!28, line width=1pt] (0.25,4.55) -- (0.65,4.55);
    \node[anchor=west, font=\scriptsize] at (0.72,4.55) {periodic eval.};
    \draw[tuwblue, very thick] (0.25,4.30) -- (0.65,4.30);
    \node[anchor=west, font=\scriptsize] at (0.72,4.30) {moving average};
  \end{tikzpicture}
  \caption{Evaluation return of the selected configuration over a 1M-step training run. The policy first solves the task at $0.44$M steps and is consistently solved from $0.54$M steps, reaching the maximum return of $500$.}
  \label{fig:learning-progress}
\end{figure}

\subsection{Loss curves}

Figure~\ref{fig:loss-curve} plots the total loss over optimizer steps for the selected run. The loss is initially driven by the value term as the critic catches up, then falls and stabilises near zero once the policy consistently solves the environment and the returns carry little surprise. The remaining small oscillations correspond to the on-policy re-collection of fresh rollouts.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[x=1cm,y=1cm]
    \def\plotw{11.1}
    \def\ploth{5.15}

    \foreach \x/\lab in {0/0,3.56/5k,7.11/10k,10.67/15k} {
      \draw[tuwgray!18] (\x,0) -- (\x,\ploth);
      \node[font=\scriptsize, below] at (\x,0) {\lab};
    }
    \foreach \y/\lab in {0/0,1.72/20,3.43/40,5.15/60} {
      \draw[tuwgray!18] (0,\y) -- (\plotw,\y);
      \node[font=\scriptsize, left] at (0,\y) {\lab};
    }

    \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (\plotw+0.25,0)
      node[below, font=\scriptsize, xshift=0.55cm] {optimizer step};
    \draw[-{Latex[length=1.5mm]}, tuwgray!80] (0,0) -- (0,\ploth+0.25)
      node[above, font=\scriptsize] {loss};

    \input{losses_1m_tikz.tex}
  \end{tikzpicture}
  \caption{Total PPO loss over optimizer steps for the selected run. The loss falls and stabilises near zero once the policy consistently solves the environment.}
  \label{fig:loss-curve}
\end{figure}

\subsection{Experiment tracking}

All training runs were logged and compared in Aim\footnote{\url{https://github.com/aimhubio/aim}}. Figure~\ref{fig:aim-dashboard} shows the eval-return and loss dashboards across the full sweep. Tracking runs in a central dashboard made it easier to compare ablations, debug suspicious metrics, and select the configurations reported above.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.49\textwidth]{aim_dashboard_eval.png}
  \hfill
  \includegraphics[width=0.49\textwidth]{aim_dashboard_loss.png}
  \caption{Aim dashboards for the full hyperparameter sweep. Left: evaluation return across runs. Right: total loss across runs.}
  \label{fig:aim-dashboard}
\end{figure}

\begin{thebibliography}{2}

\bibitem[Schulman et~al.(2017)Schulman, Wolski, Dhariwal, Radford, and Klimov]{schulman2017ppo}
John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov.
\newblock Proximal policy optimization algorithms.
\newblock \emph{arXiv preprint arXiv:1707.06347}, 2017.
\newblock \url{https://arxiv.org/pdf/1707.06347}.

\bibitem[Sutton and Barto(2018)]{sutton2018rl}
Richard S. Sutton and Andrew G. Barto.
\newblock \emph{Reinforcement Learning: An Introduction}.
\newblock MIT Press, 2nd edition, 2018.
\newblock \url{http://incompleteideas.net/book/the-book-2nd.html}.

\end{thebibliography}

\end{document}
